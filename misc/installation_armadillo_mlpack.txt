armadillo:
for installation modify src/armadillo/include/armadillo_bits/config.hpp (uncomment if neccessary!):

#define ARMA_USE_LAPACK   (needs liblapack)
#define ARMA_USE_BLAS     (needs libblas)
#define ARMA_USE_WRAPPER  (use wrapper for lapack and blas)



mlpack:
after installing libxml2-dev, do (if done with apt-get):
sudo ln -s /usr/include/libxml2/libxml /usr/include/

if installed in local include dir, do the appropriate steps
